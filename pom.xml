<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>it.unifi.ast</groupId>
	<artifactId>giommetti-ast-park-tree-project</artifactId>
	<version>0.0.1-SNAPSHOT</version>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.release>17</maven.compiler.release>

		<assertj-core-version>3.24.2</assertj-core-version>
		<assertj-swing-junit-version>3.17.1</assertj-swing-junit-version>
		<coveralls-maven-plugin-version>5.1.0</coveralls-maven-plugin-version>
		<exec-maven-plugin-version>3.6.3</exec-maven-plugin-version>
		<guice-version>6.0.0</guice-version>
		<h2-version>2.2.224</h2-version>
		<hibernate-core-version>5.6.15.Final</hibernate-core-version>
		<jacoco-maven-plugin-version>0.8.15</jacoco-maven-plugin-version>
		<javax.persistence-api-version>2.2</javax.persistence-api-version>
		<junit-version>4.13.2</junit-version>
		<maven-failsafe-plugin-version>3.6.0</maven-failsafe-plugin-version>
		<mockito-core-version>4.11.0</mockito-core-version>
		<mongo-java-driver-version>3.12.14</mongo-java-driver-version>
		<mongo-java-server-version>1.44.0</mongo-java-server-version>
		<mysql-connector-java-version>8.0.33</mysql-connector-java-version>
		<picocli-version>4.7.7</picocli-version>
		<pitest-maven-version>1.30.0</pitest-maven-version>
		<sonar-maven-plugin-version>5.7.0.6970</sonar-maven-plugin-version>
		<testcontainers-version>1.21.4</testcontainers-version>

		<sonar.organization>amerigogiommetti</sonar.organization>
		<sonar.projectKey>AmerigoGiommetti_giommetti-ast-park-tree-project</sonar.projectKey>
		<sonar.host.url>https://sonarcloud.io</sonar.host.url>

		<sonar.tests>
			${project.basedir}/src/test/java,
			${project.basedir}/src/it/java,
			${project.basedir}/src/e2e/java
		</sonar.tests>
		<sonar.junit.reportPaths>
			${project.build.directory}/surefire-reports,
			${project.build.directory}/failsafe-reports
		</sonar.junit.reportPaths>

		<sonar.coverage.exclusions>
			**/model/*.*,
			**/ParkTreeSwingApp.*,
			**/guice/**
		</sonar.coverage.exclusions>

		<!-- Rule "Tests should include assertions" (java:S2699) is a false
		     positive on our AssertJ Swing-driven tests: methods like
		     requireEnabled()/requireDisabled()/requireText() and component
		     lookups (window.label(...)) do assert GUI state, but SonarQube's
		     rule only recognizes JUnit/AssertJ/Mockito-style assertions, not
		     AssertJ Swing's fixture API. Excluded per-file, as recommended in
		     the book (Chapter "Code Quality", Section "Analysis of test code"). -->
		<sonar.issue.ignore.multicriteria>e1,e2,e3</sonar.issue.ignore.multicriteria>
		<sonar.issue.ignore.multicriteria.e1.ruleKey>java:S2699</sonar.issue.ignore.multicriteria.e1.ruleKey>
		<sonar.issue.ignore.multicriteria.e1.resourceKey>**/ParkTreeSwingViewTest.java</sonar.issue.ignore.multicriteria.e1.resourceKey>
		<sonar.issue.ignore.multicriteria.e2.ruleKey>java:S2699</sonar.issue.ignore.multicriteria.e2.ruleKey>
		<sonar.issue.ignore.multicriteria.e2.resourceKey>**/ParkTreeSwingAppJpaE2E.java</sonar.issue.ignore.multicriteria.e2.resourceKey>
		<sonar.issue.ignore.multicriteria.e3.ruleKey>java:S2699</sonar.issue.ignore.multicriteria.e3.ruleKey>
		<sonar.issue.ignore.multicriteria.e3.resourceKey>**/ParkTreeSwingAppMongoE2E.java</sonar.issue.ignore.multicriteria.e3.resourceKey>
	</properties>

	<dependencies>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>${junit-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<version>${assertj-core-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mongodb</groupId>
			<artifactId>mongo-java-driver</artifactId>
			<version>${mongo-java-driver-version}</version>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers</artifactId>
			<version>${testcontainers-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>mongodb</artifactId>
			<version>${testcontainers-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>mysql</artifactId>
			<version>${testcontainers-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>de.bwaldvogel</groupId>
			<artifactId>mongo-java-server</artifactId>
			<version>${mongo-java-server-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>javax.persistence</groupId>
			<artifactId>javax.persistence-api</artifactId>
			<version>${javax.persistence-api-version}</version>
		</dependency>
		<dependency>
			<groupId>org.hibernate</groupId>
			<artifactId>hibernate-core</artifactId>
			<version>${hibernate-core-version}</version>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<version>${h2-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>mysql</groupId>
			<artifactId>mysql-connector-java</artifactId>
			<version>${mysql-connector-java-version}</version>
		</dependency>
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-core</artifactId>
			<version>${mockito-core-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-swing-junit</artifactId>
			<version>${assertj-swing-junit-version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.google.inject</groupId>
			<artifactId>guice</artifactId>
			<version>${guice-version}</version>
		</dependency>
		<dependency>
			<groupId>com.google.inject.extensions</groupId>
			<artifactId>guice-assistedinject</artifactId>
			<version>${guice-version}</version>
		</dependency>
		<dependency>
			<groupId>info.picocli</groupId>
			<artifactId>picocli</artifactId>
			<version>${picocli-version}</version>
		</dependency>
	</dependencies>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.pitest</groupId>
					<artifactId>pitest-maven</artifactId>
					<version>${pitest-maven-version}</version>
					<configuration>
						<targetClasses>
							<param>it.unifi.ast.parktree.controller.*</param>
							<param>it.unifi.ast.parktree.repository.jpa.*</param>
							<param>it.unifi.ast.parktree.repository.mongo.*</param>
						</targetClasses>
						<targetTests>
							<!-- *Test only: excludes the src/it and src/e2e
							classes
								 (*IT / *E2E suffixes), which need Docker and would make
								 mutation analysis prohibitively slow -->
							<param>it.unifi.ast.parktree.controller.*Test</param>
							<param>it.unifi.ast.parktree.repository.jpa.*Test</param>
							<param>it.unifi.ast.parktree.repository.mongo.*Test</param>
						</targetTests>
						<mutators>
							<mutator>DEFAULTS</mutator>
						</mutators>
						<mutationThreshold>100</mutationThreshold>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.jacoco</groupId>
					<artifactId>jacoco-maven-plugin</artifactId>
					<version>${jacoco-maven-plugin-version}</version>
					<configuration>
						<excludes>
							<!-- the startup class is a pure Guice/Picocli
							wiring orchestrator: it has no business logic to test -->
							<exclude>**/ParkTreeSwingApp.*</exclude>
							<!-- the guice package only wires real
							infrastructure
								 (MongoClient, EntityManagerFactory) together: it is
								 exercised by integration/E2E tests, not unit tests -->
							<exclude>**/guice/**</exclude>
							<!-- pure data holders: fields, generated
							equals/hashCode, getters/setters — no hand-written logic to test -->
							<exclude>**/model/*.*</exclude>
						</excludes>
					</configuration>
					<executions>
						<execution>
							<goals>
								<!-- binds by default to the phase "initialize" -->
								<goal>prepare-agent</goal>
								<!-- binds by default to the phase "verify" -->
								<goal>report</goal>
							</goals>
						</execution>
						<execution>
							<id>jacoco-check</id>
							<goals>
								<!-- binds by default to the phase "verify" -->
								<goal>check</goal>
							</goals>
							<configuration>
								<rules>
									<rule>
										<element>PACKAGE</element>
										<limits>
											<limit>
												<counter>LINE</counter>
												<value>COVEREDRATIO</value>
												<minimum>0.9</minimum>
											</limit>
										</limits>
									</rule>
								</rules>
							</configuration>
						</execution>
					</executions>
				</plugin>
				<plugin>
					<groupId>org.sonarsource.scanner.maven</groupId>
					<artifactId>sonar-maven-plugin</artifactId>
					<version>${sonar-maven-plugin-version}</version>
				</plugin>
				<plugin>
					<groupId>com.github.hazendaz.maven</groupId>
					<artifactId>coveralls-maven-plugin</artifactId>
					<version>${coveralls-maven-plugin-version}</version>
					<configuration>
						<!-- the token is not hardcoded but
						it is taken from an environmental variable
						set by github through a github secret-->
						<repoToken>${env.COVERALLS_REPO_TOKEN}</repoToken>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<!-- AssertJ Swing's internal watchdog timer (ProtectingTimerTask)
				     uses reflection to read java.util.TimerTask's private state
				     field; the JDK module system blocks this by default and logs
				     an InaccessibleObjectException warning on every Swing test.
				     Re-opening java.util removes the warning without weakening
				     anything test-relevant: it is purely internal JDK encapsulation,
				     not an application security boundary. @{argLine} keeps
				     JaCoCo's own -javaagent argument (injected via prepare-agent)
				     instead of overwriting it, per JaCoCo's documented pattern for
				     combining its argLine with custom JVM args. -->
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<argLine>@{argLine} --add-opens java.base/java.util=ALL-UNNAMED</argLine>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<version>3.5.0</version>
				<executions>
					<execution>
						<id>add-test-source</id>
						<phase>generate-test-sources</phase>
						<goals>
							<goal>add-test-source</goal>
						</goals>
						<configuration>
							<sources>
								<source>src/it/java</source>
								<source>src/e2e/java</source>
							</sources>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<!-- not bound to any lifecycle phase: it only runs when
				explicitly
					 invoked with "mvn compile exec:java", it never runs as part of a
					 normal build (e.g. "mvn verify" never starts the Swing app) -->
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>${exec-maven-plugin-version}</version>
				<configuration>
					<mainClass>it.unifi.ast.parktree.app.swing.ParkTreeSwingApp</mainClass>
					<classpathScope>compile</classpathScope>
				</configuration>
			</plugin>
			<plugin>
				<!-- runs *IT.java (integration tests, src/it/java) and
				*E2E.java
					 (end-to-end tests, src/e2e/java) as part of "mvn verify", right
					 after the fast unit tests: both need Docker (Testcontainers) -->
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<version>${maven-failsafe-plugin-version}</version>
				<configuration>
					<!-- see the maven-surefire-plugin configuration above: the
					     *IT and *E2E tests drive the same AssertJ Swing robot,
					     so they hit the same java.util reflection warning. -->
					<argLine>@{argLine} --add-opens java.base/java.util=ALL-UNNAMED</argLine>
				</configuration>
				<executions>
					<execution>
						<id>default-it</id>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
					<execution>
						<id>e2e-tests</id>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
						<configuration>
							<excludes>
								<exclude>**/*IT.java</exclude>
							</excludes>
							<includes>
								<include>**/*E2E.java</include>
							</includes>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>jacoco</id>
			<activation>
				<!-- Activated when the system property "skipCoverage" is not
				defined
					 at all or is defined with a value which is not "true", thus,
					 implicitly active by default -->
				<property>
					<name>skipCoverage</name>
					<value>!true</value>
				</property>
			</activation>
			<build>
				<plugins>
					<plugin>
						<!-- configured in pluginManagement -->
						<groupId>org.jacoco</groupId>
						<artifactId>jacoco-maven-plugin</artifactId>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>mutation-testing</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-surefire-plugin</artifactId>
						<configuration>
							<!-- the Swing view tests use AssertJ Swing's GUI
							robot and are
								 excluded from PIT: they are slow and out of scope (PIT here only
								 targets Controller and Repository) -->
							<excludes>
								<exclude>**/view/swing/*Test.java</exclude>
							</excludes>
						</configuration>
					</plugin>
					<plugin>
						<!-- configured in pluginManagement -->
						<groupId>org.pitest</groupId>
						<artifactId>pitest-maven</artifactId>
						<executions>
							<execution>
								<goals>
									<goal>mutationCoverage</goal>
								</goals>
								<phase>verify</phase>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<!-- this profile deliberately runs a reduced test suite
						(no
							 Swing view tests), so JaCoCo's package coverage gate would
							 always fail on it: skip JaCoCo entirely when running PIT -->
						<groupId>org.jacoco</groupId>
						<artifactId>jacoco-maven-plugin</artifactId>
						<configuration>
							<skip>true</skip>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>coveralls</id>
			<build>
				<plugins>
					<plugin>
						<!-- configured in pluginManagement; jacoco is already
                     active by default (see the "jacoco" profile above),
                     so this profile only has to add the coveralls
                     report, which consumes jacoco's XML report -->
						<groupId>com.github.hazendaz.maven</groupId>
						<artifactId>coveralls-maven-plugin</artifactId>
						<executions>
							<execution>
								<phase>verify</phase>
								<goals>
									<goal>report</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>